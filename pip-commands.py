# pip search Pympler -> will give the details of the package, what it do
# pip install help -> will give all infor about the package.
# pip list -> list all packages
# pip freeze > requirements.txt -> will store all the required packages and version in the txt file to share
# pip uninstall package_name
# pip install -r requirment.txt -> will store all the packages in the file with version number
# pip list --outdated -> list all the outdated packages
# pip install --upgrade -> upgrade a package